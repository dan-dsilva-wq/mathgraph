import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import "katex/dist/katex.min.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MathGraph - Free Online 3D Graphing Calculator",
  description: "Graph 3D mathematical functions online for free. Visualize surfaces like z = x² + y², sin(x)cos(y), and more with interactive rotation and zoom.",
  keywords: ["3D graphing calculator", "math visualization", "surface plotter", "3D function graph", "online graphing tool"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
